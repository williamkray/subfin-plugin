need to perform a review of the codebase to ensure good standards are being followed. DRY, reusable definitions of
repeated actions, a clear feature list covering all behaviors of the codebase, test coverage, and reliable
validation patterns are key to successful modifications moving forward.

## Partial progress (2026-03-26)

### Fixed

**Null-safety: `genre` field in ItemMapper**
`FirstOrDefault()` on an empty `Genres` list returns null; strict JSON clients (Navic) can crash on
null string fields. Fixed with `?? ""` in three places:
- `ToSong` line 153: `song.Genres.FirstOrDefault() ?? ""`
- `ToAlbumShort` line 85: `album.Genres.FirstOrDefault() ?? ""`
- `ToAlbum` line 110: `album.Genres.FirstOrDefault() ?? ""`

All other required string fields (`id`, `title`, `album`, `artist`, `albumId`, `artistId`, `mediaType`,
`name`) were already guarded.

### Remaining

- DRY pass: auth boilerplate, LINQ query patterns
- Test coverage audit
- Validation patterns for required fields at entry points
